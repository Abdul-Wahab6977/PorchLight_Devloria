# Secret used to sign session JWTs — set a long random string in production.
JWT_SECRET=change-this-to-a-long-random-string

# Optional: enables real email delivery via Resend for agent inquiry notifications.
# Without this, notifications are logged to the server console and always saved
# in-app on the Inquiry record.
# RESEND_API_KEY=
